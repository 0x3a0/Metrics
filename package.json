{
  "name": "metrics-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend": "cd backend && npm run server-dev",
    "frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
